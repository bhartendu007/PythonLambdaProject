
Requirement :AWS Lambda function to downlaod excel file from web, parse it, return data in json format and store in S3 Bucket

USAGE:
   
   Go to Linux command line : ./Go.sh


About this project:

1.awslinux has been taken as base image on top of that all dependencies has been built.
2.aswcli has been used to start a lambda fucntion and push zip file with source code.